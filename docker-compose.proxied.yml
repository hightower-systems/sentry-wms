services:
  nginx-proxy:
    image: nginx:1.27-alpine
    container_name: sentry-nginx-proxy
    ports:
      - "443:443"
    volumes:
      - ./proxy/nginx.conf:/etc/nginx/conf.d/default.conf:ro
      - ./proxy/certs:/etc/nginx/certs:ro
    depends_on:
      - api
      - admin
    restart: unless-stopped
